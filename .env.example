# Vite env vars — exposed to the renderer at build time.
# Only put public keys here. Secrets go in Tauri Rust (src-tauri/) env.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
